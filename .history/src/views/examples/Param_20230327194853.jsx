import React from "react";
import { useParams } from "react-router-dom";

const Param = props => (

    const {id} = useParams()

    <div className="Param">
        <h1>Param</h1>
        <p>Valor ???!</p>
    </div>
)

export default Param